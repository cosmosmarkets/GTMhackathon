# atlas

The map service. Tiles in, vectors out.

- `src/tiles/` - the raster pipeline
- `src/vector/` - vector packing
- `src/server/` - the http surface
- `src/store/` - caches and the object store

Run `npm run bench` before you touch the packer.
